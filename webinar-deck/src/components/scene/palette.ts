// Three.js color ints mirroring the CSS design tokens (--royal, --gold /
// --signal). Kept as a single source so the 3D scenes and the 2D UI never
// drift apart.
export const ROYAL = 0x1c4e68;
export const ROYAL_LIGHT = 0x3e7a96;
export const ROYAL_DEEP = 0x0e2531;
export const GOLD = 0xe8a23d;
